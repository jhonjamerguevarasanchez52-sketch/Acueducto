# Imágenes de la app

Coloca aquí `gota_mascota.png` (la mascota GOTA que usa el botón flotante del
chat, `lib/widgets/gota_fab.dart`).

Mientras el archivo no exista, el botón muestra un icono de gota como respaldo.
